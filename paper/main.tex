% CardioWatch — PSB 2027 manuscript draft (Overleaf-ready).
% Compiles with pdfLaTeX on Overleaf as-is (standard article class).
%
% >>> PSB SUBMISSION: port the body into the World Scientific ws-procs template.
%     Steps: (1) download ws-procs (2-column) from World Scientific; (2) replace
%     the \documentclass + preamble below with the ws-procs preamble and
%     \begin{document}\markboth/title block; (3) the section/table/figure/cite
%     commands below are all standard and transfer unchanged; (4) keep <= 12
%     pages excluding title, author list, and references. Target session:
%     "AI and Machine Learning in Clinical Medicine".
%
% >>> NUMBERS: every value below traces to docs/results/<run_id>/. Cross-device
%     values come from crossdevice_stats.json (jobs 14/15); same-data values from
%     stat_tests.json / paper_tables.md. All cells are filled from the
%     pre-registered run — none are estimated.

\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{siunitx}
\usepackage[hidelinks]{hyperref}
\usepackage{authblk}
\usepackage{caption}
\usepackage{xcolor}

% Placeholder for cross-device cells awaiting the pre-registered HPC run.
\newcommand{\XX}{\textcolor{red}{\texttt{[run]}}}

\title{In-Domain Accuracy Does Not Predict Cross-Device Robustness:\\
A Pre-Registered Benchmark for Wearable Atrial Fibrillation Screening}

\author[1]{Urmi Shah}
\affil[1]{Department of Computer Engineering, San Jose State University}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Atrial fibrillation (AFib) is the most common sustained arrhythmia and a major,
largely preventable cause of ischemic stroke; a large share of cases are silent
and undiagnosed. Consumer wearables that record a single-lead electrocardiogram
(ECG) could surface these cases at population scale, but a screening model must
run unchanged on the heterogeneous fleet of devices people already own. We ask
whether the standard evaluation protocol---in-domain held-out accuracy---predicts
which model will survive that deployment. It does not. We contribute a
pre-registered, leakage-safe benchmark that scores every model on \emph{identical}
unseen windows from four hardware types, with paired DeLong tests and
patient-clustered confidence intervals. On a patient-grouped hold-out from a
hospital 12-lead corpus (CPSC~2018, $n{=}1{,}376$ records, Lead~I only), a deep
convolutional--recurrent network (CNN-LSTM) edges out a lightweight
device-agnostic model built only from heartbeat-timing features (AUC $0.949$
vs.\ $0.935$), a difference that is not statistically significant (DeLong
$p{=}0.33$, McNemar $p{=}0.40$, Holm-corrected). Moved \emph{zero-shot} onto
ambulatory Holter recordings, that ranking \emph{inverts}: the timing model
reaches AUC $0.871$ (95\% CI $0.81$--$0.92$) while the same deep model falls to
$0.773$ ($0.67$--$0.87$). On single-lead AliveCor recordings the two are
indistinguishable ($0.810$ vs.\ $0.811$, $p{=}0.93$). A deep model additionally
trained across devices recovers and leads ($0.944$ on Holter), showing the
failure is one of training distribution, not architecture. In-domain accuracy
therefore ranks these models in an order that off-device deployment reverses---a
cautionary result for wearable screening tools selected on in-domain metrics
alone. An exploratory onset-prediction analysis (104 patients) yields only modest,
sub-clinical discrimination (AUC~$0.66$) and is reported as such. All numbers come
from a single pre-registered run and are released for reproduction.
\end{abstract}

\section{Introduction}
Atrial fibrillation is the most common sustained arrhythmia and a leading cause
of ischemic stroke, yet a large fraction of cases are paroxysmal, silent, and
undiagnosed until a stroke occurs. The clinical benefit of finding AFib is
concrete and time-sensitive: timely anticoagulation substantially reduces stroke
risk~\cite{perez2019appleheart}. Because so many cases are asymptomatic, the
bottleneck is not treatment but \emph{detection at scale}, and consumer
wearables---smartwatches that record a single-lead ECG (for example, Apple~Watch
Series~4 and later)---put an opportunistic screening instrument in tens of
millions of hands. The AFib signature (absent P~waves and irregular beat-to-beat
timing) is visible in a single lead, so single-lead screening is feasible in
principle.

The obstacle we address is a \emph{deployment} problem specific to screening.
A screening model does not run on one curated device; it must run, unchanged, on
whatever hardware a population already wears---different sampling rates, gains,
electrode geometries, and noise profiles. Models trained on one recording device
frequently degrade sharply on another because they latch onto hardware-specific
waveform characteristics rather than the underlying cardiac pattern; we call this
the \emph{cross-device gap}. For population screening, robustness to this gap is
not a secondary metric---it determines who actually gets caught.

This paper asks a question about \emph{evaluation} rather than architecture: does
in-domain held-out accuracy---the metric on which wearable screening models are
routinely selected---predict which model will still work once it leaves its
training device? Our contributions are:
\begin{enumerate}
  \item A \textbf{cautionary benchmark finding}: the in-domain ranking of two
  models \emph{inverts} under device shift. The deep model that is (marginally)
  ahead on matched hospital data falls well behind the device-agnostic model on
  unseen ambulatory Holter recordings. Selecting on in-domain accuracy would
  choose the model that generalizes worse.
  \item A \textbf{pre-registered, leakage-safe benchmark protocol} for wearable
  AFib screening---frozen patient-grouped hold-out, frozen thresholds, zero-shot
  external cohorts scored on identical windows, and a pre-declared family of
  paired significance tests with patient-clustered confidence intervals---directly
  responsive to documented leakage and reproducibility problems in medical machine
  learning~\cite{kapoor2023leakage}.
  \item A \textbf{three-way characterization of the cross-device gap}: a deep model
  trained on one device collapses off-device; a device-agnostic timing
  representation degrades gracefully with no cross-device training; and a deep
  model trained \emph{across} devices recovers and leads. The failure is one of
  training distribution, not architecture.
  \item An \textbf{honest exploratory study} of AFib \emph{onset} prediction from
  pre-onset sinus rhythm, reported with its limitations.
\end{enumerate}
We scope the work to AFib detection and stroke prevention; it does not address
heart-attack (myocardial infarction) prediction, a different condition with a
different ECG signature.

\section{Related Work}
Single-lead AFib detection has been studied since the PhysioNet/CinC 2017
challenge on short AliveCor recordings~\cite{clifford2017af}, and large
prospective work established that smartwatch screening surfaces undiagnosed
AFib~\cite{perez2019appleheart}. Recent deep models report very high in-domain
accuracy, and---directly relevant to us---Ben-Moshe et
al.~\cite{benmoshe2024rawecgnet} show that a carefully trained raw-ECG network can
be made to \emph{generalize} across leads and cohorts, and argue that raw-waveform
models can outperform hand-crafted RR-interval features. We do not dispute this:
with cross-device training and enough data, learned representations can close the
gap. Our claim is narrower and complementary. First, the realistic screening
constraint is \emph{zero-shot} transfer---a deployed model rarely gets retrained
per device---and under that constraint we find timing features transfer without
any cross-device training. Second, we make the comparison on \emph{matched} data
with a pre-registered paired test, rather than comparing models trained on
different corpora. Bahrami~Rad et al.~\cite{bahramirad2024crowdsourced} similarly
report that timing-based features transfer across AliveCor and Apple~Watch better
than learned waveform features; we add a same-data statistical comparison and a
multi-cohort, clustered-CI cross-device evaluation.

Methodologically, our design responds to the leakage-driven reproducibility
crisis in ML-based science~\cite{kapoor2023leakage}: we pre-register the hold-out
and tests before training. Onset prediction has a long history beginning with the
PAF Prediction Challenge~\cite{moody2001paf}; we revisit it on continuous Holter
data with patient-grouped evaluation and treat it as exploratory.

\section{Data and Methods}

\subsection{Datasets}
We use five public or personally exported sources. CPSC~2018 (within the
PhysioNet/CinC~2020 corpus~\cite{alday2020classification}) provides hospital
12-lead ECGs, from which we take Lead~I only. PhysioNet/CinC~2017 provides
single-lead AliveCor recordings~\cite{clifford2017af}. The MIT-BIH Atrial
Fibrillation Database (\texttt{afdb}) and the Long-Term AF Database
(\texttt{ltafdb}) provide annotated ambulatory Holter
recordings~\cite{moody1983afdb,petrutiu2007ltaf,goldberger2000physionet}. A small
set of personal Apple~Watch exports (54 recordings, four volunteers, one confirmed
AFib) is used only for real-world validation; its labels are the watch's own
classifier output, not independent clinical adjudication. A separate tabular
clinical risk dataset is used for an additional analysis (Section~\ref{sec:extra})
and is reported as a distinct cohort.

\subsection{Models}
The \textbf{device-agnostic model} (RR~+~RF) detects AFib from heartbeat-timing
features only---coefficient of variation, RMSSD, pNN50, sample entropy, and
related statistics of the RR-interval series---fed to a random forest. Because
these features measure timing rather than waveform shape, they are insensitive to
device sampling rate and gain. The \textbf{deep model} (CNN-LSTM) is a three-block
1D convolutional encoder followed by a two-layer LSTM and a linear head, trained
on 10-second Lead~I windows.

\subsection{Pre-registered evaluation protocol}
To avoid comparing models trained on different data, and to guard against the
test-set leakage that undermines reproducibility in medical
ML~\cite{kapoor2023leakage}, we fixed the analysis before training. We created a
deterministic, label-stratified hold-out of CPSC records (20\%, fixed seed) after
an explicit one-record-per-patient check, and excluded these records from
\emph{both} models' training. The device-agnostic model and a CPSC-only deep model
were then scored on the identical hold-out. Decision thresholds were frozen on
training/validation data, never on the hold-out. The primary discrimination test
is the paired DeLong test on AUC~\cite{delong1988comparing,sun2014fast}; the
primary threshold-level test is McNemar's test on correctness vectors. A single
pre-registered Holm family~\cite{holm1979simple} (DeLong plus fixed-threshold
McNemar across the model-versus-model pairs) controls multiple comparisons.
Class imbalance during training was handled with SMOTE applied \emph{inside} each
cross-validation fold~\cite{chawla2002smote}. A pre-registered amendment adds the
cross-device family below; the same-data primary is unchanged.

\subsection{Zero-shot cross-device evaluation}
The screening question is answered by moving the \emph{already-trained} models,
with no retraining or fine-tuning, onto unseen hardware. For each external cohort
we score RR~+~RF and the CPSC-only CNN-LSTM on the \emph{identical} 10-second
Lead~I windows, so the comparison is truly paired: long Holter records are cut
into consecutive 10-second windows and each window is labeled by the active rhythm
at its midpoint. Per cohort with both classes present we run a paired DeLong test
of RR~+~RF versus CNN-LSTM(CPSC); these cross-device tests form a single
pre-registered Holm family, separate from the same-data family. Every external AUC
is reported with a \textbf{patient-/record-clustered bootstrap} 95\% CI (resampling
whole patients, since windows within a patient are correlated). The
data-asymmetric combined CNN-LSTM is scored only on cohorts it never saw during
training (it is omitted for CinC~2017 to avoid train-on-test leakage).

\subsection{Split-sensitivity (pre-registered, not completed)}
The pre-registered plan included a secondary sensitivity analysis repeating the
hold-out\,$\rightarrow$\,train\,$\rightarrow$\,paired-eval chain over 20 alternate
hold-out seeds. This analysis did not complete within the available compute budget
and is therefore \emph{not} reported; the same-data null rests on the single
pre-registered seed and should be read with that limitation in mind.

\subsection{Exploratory onset prediction}
Separately, we test whether imminent AFib can be predicted from sinus rhythm. From
continuous Holter records we label 30-second sinus windows as \emph{pre-onset}
(onset within 30~minutes) or \emph{control} (no onset within 60~minutes), excluding
windows already in AFib. Features combine RR/HRV statistics with
neurokit2~\cite{makowski2021neurokit} atrial-ectopy and expanded HRV markers, each
z-scored against the patient's own control-window baseline. The pre-registered
primary is the neurokit-feature variant; RR-only and P-wave variants are reported
as labeled sensitivity analyses. Evaluation uses patient-grouped cross-validation
pooled over \texttt{afdb} and \texttt{ltafdb}.

\section{Results}

\subsection{The in-domain ranking inverts off-device (main result)}
Figure~\ref{fig:inversion} and Table~\ref{tab:crossdevice} report the zero-shot
head-to-head on identical windows.

\begin{figure}[t]
\centering
% docs/results/<run_id>/figures/rank_inversion.png (make_figures.py)
\includegraphics[width=0.82\linewidth]{figures/rank_inversion.png}
\caption{The in-domain ranking does not survive device shift. Cohorts are ordered
by increasing shift from the training distribution. On the matched hospital
hold-out the deep model is nominally ahead; on single-lead AliveCor the two are
indistinguishable; on ambulatory Holter the ordering has reversed. Error bars are
patient/record-clustered bootstrap 95\% CIs. Apple~Watch is omitted (single
confirmed positive; plausibility only).}
\label{fig:inversion}
\end{figure} On matched hospital data the deep model is marginally ahead
($0.949$ vs.\ $0.935$, not significant). Moved onto ambulatory Holter recordings
the ordering reverses: the device-agnostic timing model reaches AUC $0.871$
(clustered 95\% CI $0.81$--$0.92$) while the same deep model falls to $0.773$
($0.67$--$0.87$), a paired difference of $+0.098$ in favour of the timing model.
On single-lead AliveCor recordings the two models are indistinguishable
($0.810$ vs.\ $0.811$; $p{=}0.93$), so the advantage is cohort-dependent rather
than universal. A deep model additionally trained across devices recovers and
leads on both external cohorts where it can be scored without train-on-test
contamination (Holter AUC $0.944$, $0.92$--$0.97$), indicating that the failure of
the single-device deep model is one of training distribution, not architecture.

\emph{Statistical caveat.} The Holter cohort contributes $84{,}295$ windows from
only $23$ patients. DeLong's variance estimator assumes independent samples, so
its nominal $p$ (numerically $0$ here, $z{=}51$) is badly anticonservative under
this within-patient correlation. We therefore treat the patient-clustered
bootstrap CIs as the primary inferential quantity and report DeLong as a
secondary, clearly non-independent statistic; the clustered intervals for the two
models overlap modestly, which is the honest strength of the effect.

The Apple~Watch cohort contains a single confirmed positive, so its DeLong test is
undefined and its AUC is a plausibility check---not a discrimination claim.
Long-Term AF (\texttt{ltafdb}) was pre-registered but is \emph{excluded} from the
scored table: exhaustive per-window scoring of $84$ multi-hour records
($\sim$725{,}000 windows) exceeded the compute budget on three attempts. MIT-BIH
provides the ambulatory-Holter evaluation; we report the exclusion rather than
substitute an unplanned subsample.

\begin{table}[t]
\centering
\caption{Zero-shot cross-device head-to-head on identical 10\,s Lead~I windows,
with the in-domain hold-out shown for reference. AUCs carry patient/record-clustered
bootstrap 95\% CIs. $\Delta$AUC is (timing $-$ deep); positive favours the
device-agnostic model. DeLong $p$ is Holm-corrected across the cross-device family
and is secondary to the clustered CIs (see caveat).}
\label{tab:crossdevice}
\small
\begin{tabular}{llccccc}
\toprule
Cohort & Device & Records & RR~+~RF & CNN-LSTM(CPSC) & $\Delta$AUC & DeLong $p$ \\
\midrule
CPSC hold-out (in-domain) & Hospital 12-lead & 1{,}376 & 0.935 & \textbf{0.949} & $-0.013$ & 0.33 \\
\midrule
MIT-BIH \texttt{afdb} & Holter 250\,Hz & 23 & \textbf{0.871} & 0.773 & $+0.098$ & $<0.001^{\ddagger}$ \\
CinC 2017 & AliveCor 300\,Hz & 8{,}521 & 0.810 & 0.811 & $-0.001$ & 0.93 \\
Apple~Watch$^{\dagger}$ & Wearable 512\,Hz & 4 & 0.792 & 0.642 & --- & n/a \\
\bottomrule
\end{tabular}

\vspace{2pt}
{\footnotesize $^{\dagger}$Single confirmed positive; plausibility check only.
$^{\ddagger}$Nominal DeLong $p$; anticonservative under within-patient correlation
(23 patients), see caveat. Cross-device deep model trained across devices:
Holter $0.944$ ($0.92$--$0.97$), Apple~Watch $0.962$; omitted on CinC~2017 to
avoid train-on-test.}
\end{table}

\subsection{Controlled same-data comparison}
On the pre-registered hold-out (Table~\ref{tab:ecg}), the device-agnostic timing
model and the CPSC-only deep model are statistically indistinguishable: the AUC
difference is $-0.013$ (95\% CI $-0.032$ to $0.006$), DeLong $p{=}0.33$ and McNemar
$p{=}0.40$ after Holm correction. This is the ranking that inverts off-device
(Table~\ref{tab:crossdevice}): in-domain the deep model is nominally ahead, yet it
is the model that fails to transfer. The combined deep model, additionally trained
on wearable data, reaches AUC $0.975$ and is significantly higher, but this is a
deployment comparison with more and different training data, not a same-data test.
The split-sensitivity analysis over alternate seeds did not complete
(Section~\ref{sec:extra} and Limitations), so this null rests on the single
pre-registered split.

\begin{table}[t]
\centering
\caption{ECG models on the pre-registered CPSC hold-out ($n{=}1{,}376$, Lead~I).
Thresholds frozen on non-hold-out data.}
\label{tab:ecg}
\begin{tabular}{lccccc}
\toprule
Model & AUC & Recall & Specificity & F1 & Threshold \\
\midrule
RR~+~RF (device-agnostic) & 0.935 & 0.803 & 0.879 & 0.679 & 0.40 \\
CNN-LSTM (CPSC only) & 0.949 & 0.959 & 0.832 & 0.701 & 0.40 \\
CNN-LSTM (combined, deployment) & 0.975 & 0.918 & 0.941 & 0.837 & 0.40 \\
Random baseline & 0.502 & 0.508 & 0.511 & 0.269 & 0.50 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Exploratory onset prediction}
Predicting AFib before it begins is a harder, separate problem. Pooled across two
Holter cohorts (104 patients, patient-grouped cross-validation), personalized
timing and atrial-ectopy features give modest, consistent pre-onset discrimination
(neurokit variant, window AUC $0.66$). At about $0.5$ false alarms per hour the
model warns of a minority of onsets a median of $\sim$1--2 minutes ahead.
Per-patient normalization was essential. We report this as a real but sub-clinical
signal, not a deployable early-warning system; the RR-only ($0.606$) and P-wave
variants are sensitivity analyses.

\subsection{Additional analysis: tabular clinical risk (separate cohort)}
\label{sec:extra}
For completeness we also train tabular clinical risk models (random forest, AUC
$0.954$; gradient boosting, AUC $0.927$) on a distinct patient population. These
are \emph{not} combined with the ECG models: the cohorts do not overlap, so any
fused score would be unvalidated. We therefore report them as a separate cohort
and treat clinical$+$ECG fusion as out of scope until same-patient paired data are
available.

\section{Discussion}
The practical message is about \emph{model selection}, not architecture. If a
wearable screening model is chosen the way such models are usually chosen---by
in-domain held-out accuracy---this study would have selected the CNN-LSTM, which
was nominally ahead on matched hospital data. That model is the one that fell to
AUC $0.773$ on ambulatory Holter, while the model it beat in-domain reached
$0.871$. In-domain accuracy did not merely overstate off-device performance; it
ordered the two candidates incorrectly. For screening on a device fleet that
cannot be retrained per watch, this argues for making zero-shot cross-device
evaluation part of model selection rather than a post-hoc robustness check.

The effect is cohort-dependent and we do not overstate it. On single-lead
AliveCor recordings the two representations are indistinguishable
($p{=}0.93$)---closer in acquisition to the training corpus than a wrist-worn
Holter---so the inversion appears where the device shift is largest. Nor is this
an argument against deep learning: a CNN-LSTM trained \emph{across} devices led on
every external cohort where it could be scored honestly. That is consistent with
work showing raw-ECG networks generalize well when trained
appropriately~\cite{benmoshe2024rawecgnet}; the failure we document belongs to the
single-device training distribution, and the device-agnostic representation is
best read as a strong, interpretable default when cross-device training data are
unavailable.

Several limitations bound these claims. The Holter evidence rests on 23 patients;
we report patient-clustered bootstrap CIs, and the nominal DeLong $p$ is
anticonservative under within-patient correlation, so the strength of that single
comparison should not be overread. The pre-registered split-sensitivity analysis
over alternate seeds did not complete, so the same-data null rests on one split.
The Long-Term AF cohort was pre-registered but could not be scored exhaustively
within budget and is reported as excluded rather than subsampled post hoc. The
Apple~Watch cohort has one confirmed positive and device-generated labels, so it
supports plausibility only. The tabular clinical models use a different population
and are not a cross-modal ablation. The onset analysis uses cohorts of selected
AFib patients and shows only modest discrimination; richer atrial markers, longer
pre-onset context, and purpose-built data are the natural next steps.

\section{Conclusion}
For wearable AFib screening, the metric that selects models and the metric that
predicts deployment are not the same. We show a concrete case in which the
in-domain ranking of two models inverts under device shift, and we contribute a
pre-registered, leakage-safe benchmark---identical windows, zero-shot external
cohorts, paired tests, patient-clustered intervals---that makes such inversions
visible before deployment rather than after. The failure is one of training
distribution rather than architecture: a deep model trained across devices
recovers and leads. Predicting AFib before it begins remains hard; our
personalized features yield a real but sub-clinical early-warning signal that
motivates future work rather than a deployable alarm.

\section*{Reproducibility}
All reported numbers come from one pre-registered run. Code, the analysis plan
(including the cross-device amendment), the hold-out manifest checksum, and the
compact result files are available in the project repository; a versioned snapshot
will be archived with a DOI.

\bibliographystyle{unsrt}
\bibliography{references}

\end{document}
